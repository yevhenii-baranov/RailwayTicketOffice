@model List<OrderModel>

@{
    ViewBag.Title = "Orders";
    Layout = "_Layout";
}

<h2>My orders</h2>

@if (Model.Count == 0)
{
    <div>
        <h2 class="text-center align-middle">Your order list is empty.</h2>
        <h2 class="text-center align-middle">Buy something!</h2>
    </div>
}
else
{
    <div>kek</div>
}